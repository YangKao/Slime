const description = [];
description.push('Suppose  $f$  is a continuous real-valued function defined on the real interval $[a, b]$. There exists a sequence of polynomial $P_n$ such that $\\lim_{n\\to\\infty}P_n=f$.');

export default {
  title: "Descartes' Life",
  description: description[Math.round(Math.random() * (description.length - 1))]
};
